# Wiki Index

> 由 `python scripts/wiki.py index --write` 从页面元数据生成；请修改页面的 title / summary / aliases，不手改本表。
> 知识是候选证据；draft 不代表用户已审定。任务发现使用 maps/task-routing.md 或 discover 命令。

## Maps

- [[maps/task-routing|Task routing]] — Optional thin routes from task language to active knowledge pages. · 检索词：task routing / route map

## 审计

- [[queries/query-log|检索影响记录]] — 同批知识维护的使用证据；运行事件与待处理反馈用 `python scripts/wiki.py pending`。
